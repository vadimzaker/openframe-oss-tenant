apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: openframe-authorization-server
build:
  artifacts:
    - image: ghcr.io/flamingo-stack/openframe-oss-tenant/openframe-authorization-server
      context: ../../../
      sync: 
        manual:
          - src: "**/*"
            dest: "/app"
      docker:
        dockerfile: openframe/services/openframe-authorization-server/Dockerfile
        buildArgs:
          GITHUB_ACTOR: "{{.GITHUB_ACTOR}}"
          GITHUB_TOKEN: "{{.GITHUB_TOKEN}}"
      hooks:
        before:
          - command: [ "mvn", "clean", "package", "-DskipTests"]
  local:
    useDockerCLI: false
    useBuildkit: true
    push: false
manifests:
  helm:
    releases:
      - name: openframe-authorization-server
        chartPath: ../../../manifests/microservices/openframe-authorization-server
        valuesFiles:
          - ../../../manifests/microservices/openframe-authorization-server/values.yaml
        # setValues:
