apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: openframe-external-api
build:
  artifacts:
    - image: ghcr.io/flamingo-stack/openframe-oss-tenant/openframe-external-api
      context: ../../../
      sync: 
        manual:
          - src: "**/*"
            dest: "/app"
      docker:
        dockerfile: openframe/services/openframe-external-api/Dockerfile
        buildArgs:
          GITHUB_ACTOR: "{{.GITHUB_ACTOR}}"
          GITHUB_TOKEN: "{{.GITHUB_TOKEN}}"
      hooks:
        before:
          - command: [ "mvn", "clean", "package", "-DskipTests"]
  local:
    useDockerCLI: false
    useBuildkit: true
    push: false
manifests:
  helm:
    releases:
      - name: openframe-external-api
        chartPath: ../../../manifests/microservices/openframe-external-api
        valuesFiles:
          - ../../../manifests/microservices/openframe-external-api/values.yaml
        # setValues:
